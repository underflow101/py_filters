from setuptools import setup

setup(name='py_sensor_filters',
      version='0.1.1.post1',
      description='Fast Signal Filters Wrapped in Python',
      packages=['kalman_filter'],
      author='underflow101',
      author_email='ikarus125@gmail.com',
      zip_safe=False)